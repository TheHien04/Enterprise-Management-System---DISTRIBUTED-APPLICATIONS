import PlaceholderPage from '@/components/common/PlaceholderPage'
export default function CustomersPage() {
  return <PlaceholderPage title="Customers" subtitle="UC-01 — Enterprise customer management (Req 4.1)" items={['Create / update / suspend customer', 'Lookup related contracts and billing']} />
}
