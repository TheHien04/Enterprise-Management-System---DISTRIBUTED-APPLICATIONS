import PlaceholderPage from '@/components/common/PlaceholderPage'
export default function BillingPage() {
  return <PlaceholderPage title="Billing Wizard" subtitle="UC-06 — Draft → Calculate → Reconcile → Submit" items={['Show snapshot unit_price', 'E-sign / Issued banners']} />
}
